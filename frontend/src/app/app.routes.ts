import { Routes } from '@angular/router';
import { PoetComponent} from "./poet/poet.component";

export const routes: Routes = [
  { path: 'poet-component', title: 'Poet Component', component: PoetComponent }
];
